# Core API config
# Supply your own provider/API key. The code is wired for OpenAI Agents SDK by default.
# For OpenAI: set OPENAI_API_KEY. If using a custom base URL (proxies/self-hosted), set OPENAI_BASE_URL.
OPENAI_API_KEY=
OPENAI_BASE_URL=

# Models
LLM_MODEL=gpt-5-mini
VISION_MODEL=gpt-5-mini

# Performance
MAX_CONCURRENCY=200

# Provider selection
# Default is OpenAI Agents SDK. Implement and register others in providers/.
PROVIDER=openai_agents
